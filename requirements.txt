# Core inference
torch>=2.2,<3
torchvision>=0.17
transformers>=4.40
pillow>=10.0
numpy>=1.24
# KB + config + CLI + API
pyyaml>=6.0
typer>=0.12
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
# Web UI. Python 3.9 caps us at gradio 4.x (gradio 5 needs Python >=3.10).
gradio==4.44.1
gradio_client==1.3.0
# Optional LLM providers (adapters import lazily; not required to run)
# openai>=1.0
# anthropic>=0.30
# Tests
pytest>=8.0
